# ============================================================
# 开发环境配置 (Windows本地开发)
# ============================================================

# === 环境标识 ===
FLASK_ENV=development
FLASK_DEBUG=True

# === 数据库连接配置 - 本地MySQL ===
SQLALCHEMY_DATABASE_URI=mysql+pymysql://root:exak472008@localhost:3306/caigou?charset=utf8mb4

# 分离的数据库配置(备用)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=exak472008
DB_NAME=caigou

# 禁用 Flask 对象修改追踪，避免不必要的内存消耗
SQLALCHEMY_TRACK_MODIFICATIONS=False

# === 应用密钥（开发环境用简单的） ===
APP_SECRET=dev_secret_key_for_testing_only

# === 本地大模型（Ollama） ===
LLM_BASE=http://localhost:11434
LLM_MODEL=qwen2.5:7b

# === 失败队列提醒（可选） ===
RFQ_FAILURE_NOTIFY_CRON=0 10 * * *

# === Celery / Redis 异步任务 - 本地 ===
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/1
CELERY_TASK_DEFAULT_QUEUE=default
CELERY_WORKER_CONCURRENCY=4
CELERY_TASK_SOFT_TIME_LIMIT=30
CELERY_TASK_TIME_LIMIT=60

# === 企业微信配置（开发环境 - 暂时禁用或使用测试配置） ===
# 开发环境建议：如果无法本地测试企业微信，可以先注释掉
WEWORK_CORP_ID=ww7f7bb9e8fc040434
WEWORK_AGENT_ID=1000010
WEWORK_SECRET=e6zeeDUO7Jp1BB-T8Nu-l0Kmn84lRcpHUgig8eff8uQ

# H5页面域名（开发环境使用本地地址）
WEWORK_H5_DOMAIN=http://localhost:3000

# 后端API域名（开发环境使用本地地址）
BACKEND_DOMAIN=http://localhost:5001

# === 微信服务号配置（开发环境 - 可选） ===
WECHAT_APPID=wx04440e471f2447c3
WECHAT_APPSECRET=fb3a210a4614a788be4378862ea5db9a
WECHAT_TOKEN=caigou_wechat_2025
WECHAT_ENCODING_AES_KEY=ocyhYQQdomEiDdfq3Is4twXaB2RWysMjaPLeeQcM6oe

# === 开发环境特有配置 ===
# 开发模式下显示详细错误信息
DEBUG=True

# 日志级别
LOG_LEVEL=DEBUG

# 允许跨域（开发环境）
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# 开发环境备注
# 注意：本配置文件用于Windows本地开发
# 所有服务都在localhost上运行
# 企业微信回调无法在本地环境测试，需要在生产环境测试
