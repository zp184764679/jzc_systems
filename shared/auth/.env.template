# ============================================
# JZC SSO 认证配置模板
# ============================================
#
# 重要说明:
# 所有子系统必须使用相同的 JWT_SECRET_KEY 和 AUTH_DB_NAME
# 配置不一致会导致用户登录后无法访问其他子系统
#
# 正确配置流程:
# 1. 从 Portal/backend/.env 复制 JWT_SECRET_KEY
# 2. 确保 AUTH_DB_NAME 为 cncplan
# 3. 重启后端服务
#
# ============================================

# ----------------------------------------
# JWT 配置 - 必须与 Portal 一致
# ----------------------------------------
# 从 Portal/backend/.env 复制此值
# 生产环境必须设置，开发环境可选
SECRET_KEY=<从 Portal 复制>
JWT_SECRET_KEY=<从 Portal 复制>

# ----------------------------------------
# 认证数据库 - 所有系统统一使用 cncplan
# ----------------------------------------
AUTH_DB_USER=app
AUTH_DB_PASSWORD=app
AUTH_DB_HOST=localhost
AUTH_DB_NAME=cncplan

# ============================================
# 以下为各系统独有配置
# 请根据系统需求修改
# ============================================

# Flask 环境
FLASK_ENV=development

# 业务数据库配置 (各系统可能不同)
# DB_HOST=localhost
# MYSQL_USER=app
# MYSQL_PASSWORD=app
# MYSQL_DATABASE=<系统专用数据库>

# 服务端口 (各系统不同)
# Portal: 3002
# HR: 8003
# Account: 8004
# 报价: 8001
# 采购: 5001
# SHM: 8006
# CRM: 8002
# PORT=<端口号>

# CORS 配置
# CORS_ORIGINS=http://localhost:<前端端口>,https://jzchardware.cn
